# Exemplo de variáveis de ambiente para backend
# Copie este arquivo para `backend/.env` e defina os valores reais (NÃO COMITAR)

# Configuração de e-mail (SMTP)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=465
MAIL_USERNAME=seu-email@gmail.com
MAIL_PASSWORD=sua_senha_de_app_aqui
MAIL_USE_TLS=False
MAIL_USE_SSL=True

# Chave JWT (defina uma string segura em produção)
JWT_SECRET_KEY=uma_chave_super_secreta_aqui

# Banco de dados (exemplo)
DATABASE_URL=
